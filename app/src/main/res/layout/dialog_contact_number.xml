<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:paddingHorizontal="24dp"
    android:paddingTop="28dp"
    android:paddingBottom="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Order contact number"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#1A1A1A"
        android:layout_marginBottom="24dp" />

    <!-- Phone input row with rounded border -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/phoneInputCard"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="24dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:strokeColor="#FF6B00"
        app:strokeWidth="1.5dp"
        app:cardBackgroundColor="#FFFFFF">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="14dp">

            <!-- +63 prefix (always shown, non-editable) -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+63"
                android:textSize="16sp"
                android:textColor="#AAAAAA"
                android:layout_marginEnd="6dp" />

            <!-- Thin vertical divider -->
            <View
                android:layout_width="1dp"
                android:layout_height="20dp"
                android:background="#DDDDDD"
                android:layout_marginEnd="10dp" />

            <!-- Number input (digits only, auto-formatted with spaces) -->
            <EditText
                android:id="@+id/inputPhone"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:inputType="phone"
                android:hint="XXX XXX XXXX"
                android:textColorHint="#CCCCCC"
                android:textSize="16sp"
                android:textColor="#1A1A1A"
                android:background="@null"
                android:maxLength="12"
                android:singleLine="true"
                android:imeOptions="actionDone" />

            <!-- Clear button -->
            <ImageButton
                android:id="@+id/btnClearPhone"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_close"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="#AAAAAA"
                android:padding="6dp"
                android:visibility="gone" />

            <!-- Contacts picker icon -->
            <ImageButton
                android:id="@+id/btnPickContact"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_contacts_book"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:padding="4dp"
                android:layout_marginStart="2dp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Update button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnUpdatePhone"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Update"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:textSize="17sp"
        app:cornerRadius="12dp"
        android:backgroundTint="#FF6B00" />

</LinearLayout>
